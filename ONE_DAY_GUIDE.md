# 一天吃透性能测试：从架构到面试

这份手册按照“先能跑，再能解释，最后能定位”的顺序设计。你不需要一天背完所有工具参数，但必须能把一次性能测试讲成一条闭环证据链。

## 0. 先记住一条主线

面试官真正想知道的不是“会不会点 Locust”，而是你能否回答：

> 在什么业务目标下，以什么流量模型压哪些链路，系统的容量边界在哪里，哪里先变慢，如何用数据证明原因，优化后如何证明没有回归？

项目里的主链路：

```text
用户目标 -> SLO -> 流量模型 -> 场景脚本 -> 负载发生器
   -> API/应用 -> Redis/Postgres/支付依赖 -> 指标/日志/Trace
   -> 基线对比 -> 瓶颈假设 -> 单变量验证 -> 优化 -> 回归
```

任何一个环节缺失，结论都不完整。例如只说“压了 100 并发，P95 是 300 ms”，却没有说明请求比例、持续时间、数据量、错误率和机器规格，这个数字无法复现，也不能支撑上线决策。

## 1. 一天时间表

### 08:30-09:00：建立概念框架

掌握四个维度：

- **负载（Load）**：并发用户数、到达率、业务比例、数据规模。
- **性能（Performance）**：延迟、吞吐、错误率、资源利用率。
- **容量（Capacity）**：在 SLO 仍满足时的最大稳定吞吐或并发。
- **稳定性（Stability）**：长时间运行后是否出现内存增长、连接池耗尽、队列堆积或错误率升高。

性能测试类型：

| 类型 | 问题 | 典型配置 |
| --- | --- | --- |
| 基准测试 | 单接口和健康状态是多少 | 低并发、短时、固定数据 |
| 负载测试 | 目标业务流量能否满足 SLO | 目标 RPS/并发，10-30 分钟 |
| 压力测试 | 超过目标后如何退化 | 阶梯增加，直到 SLO 破坏 |
| 峰值测试 | 突然流量是否恢复 | 突增/突降，观察队列和熔断 |
| 稳定性测试 | 运行几小时会不会泄漏 | 固定负载，1-24 小时 |
| 容量测试 | 最大稳定容量是多少 | 找到拐点并留出安全余量 |

### 09:00-10:00：读懂本项目架构

先启动：

```powershell
docker compose up --build -d
```

然后分别访问 `/docs`、Locust、Prometheus、Grafana。对应关系如下：

| 层 | 本项目 | 你要观察什么 |
| --- | --- | --- |
| 负载层 | Locust `MallUser` | 业务权重、请求命名、失败判定 |
| 网关/服务层 | FastAPI | 路由耗时、状态码、并发处理能力 |
| 缓存层 | Redis mock dependency | 命中路径和依赖延迟 |
| 数据库层 | Postgres mock dependency | 搜索慢查询和写入耗时 |
| 下游层 | payment mock dependency | 长尾延迟和 502 |
| 观测层 | Prometheus/Grafana | RED、依赖 P95、错误比率 |
| 分析层 | `analyze_results.py` | 统计事实、假设和下一轮实验 |

服务并没有真的连接 Redis/Postgres，而是用可控延迟模拟依赖。这是为了让你在一天内看懂“应用耗时是依赖耗时之和”，而不被环境安装和数据准备拖住。生产项目应换成真实中间件或测试环境服务，并保留同样的指标命名。

### 10:00-11:00：从需求推导流量模型

假设产品给出：大促峰值 10 万日活，峰值 15 分钟，平均每个活跃用户每分钟浏览 6 次、搜索 1 次、详情 3 次、加购 0.5 次、下单 0.1 次。

先估算到达率：

```text
峰值活跃用户 = 100000 * 峰值占比（例如 8%）= 8000
浏览 RPS = 8000 * 6 / 60 = 800
搜索 RPS = 8000 * 1 / 60 = 133
详情 RPS = 8000 * 3 / 60 = 400
加购 RPS = 8000 * 0.5 / 60 = 67
下单 RPS = 8000 * 0.1 / 60 = 13
总业务 RPS 约 1413
```

这是业务到达率，不是 Locust 用户数。并发近似为 Little's Law：

```text
并发数 ≈ 到达率（RPS） * 平均响应时间（秒）
```

若平均响应 0.4 秒，1413 RPS 需要约 565 个在途请求；再按等待时间、客户端和安全余量扩大。不要把“500 用户”直接当成“500 RPS”。

项目的权重为浏览 35、搜索 20、详情 25、加购 12、下单 8，总和 100。这个权重是演示用，面试时应说明它来自产品漏斗或线上访问日志，而不是凭感觉。

### 11:00-12:00：理解 Locust 脚本

打开 `locust/locustfile.py`，逐段看：

1. `HttpUser` 是一个虚拟用户，不等于一个请求线程；每个用户循环执行任务并按 `wait_time` 思考。
2. `@task(35)` 是相对权重，五个任务的比例约为 35%/20%/25%/12%/8%。
3. `name="GET /api/products"` 把带不同参数的请求聚合成业务接口，避免报告被 URL 参数打散。
4. `catch_response=True` 后，HTTP 200 之外的业务错误必须显式 `failure()`；否则只看网络层成功会漏掉错误。
5. `on_start()` 为每个虚拟用户生成身份和商品 ID，避免所有请求打同一个缓存键。

第一次实验：

```powershell
docker compose run --rm locust `
  -f /mnt/locust/locustfile.py --host http://service:8000 `
  --headless -u 20 -r 5 -t 2m --csv /mnt/results/baseline
```

第二次只把 `-u` 改成 50，第三次改成 100。每次保留 CSV，不要覆盖结果。

### 13:00-14:00：理解指标，不被平均值误导

必须同时看：

- **吞吐量**：RPS/TPS，系统每秒完成多少请求。
- **延迟**：平均值、P50、P90、P95、P99。P99 代表最慢 1% 请求，支付、登录等关键链路尤其重要。
- **错误率**：`失败请求数 / 总请求数`，并按 4xx、5xx、超时和业务失败拆分。
- **资源**：CPU、内存、GC、线程/协程、连接池、队列、磁盘 IO、网络 IO。
- **依赖**：Redis/Postgres/HTTP 下游各自的调用次数、耗时、错误和连接池等待。

常见判断：

```text
P99 / P50 很大 -> 长尾问题，查慢查询、锁、GC、下游重试或队列
吞吐不再增加，延迟持续上升 -> 到达容量拐点
CPU 低但请求超时 -> 可能在等待 IO、锁、连接池或外部服务
错误率升高且下游错误同步升高 -> 依赖故障传播
平均值正常但 P99 超 SLO -> 不能判定通过
```

Grafana 的四张图分别对应 request rate、应用 P95、error ratio、dependency P95。先看同一时间窗口的相关性，再下结论。

### 14:00-15:00：用阶梯测试找容量拐点

推荐阶梯：20 -> 50 -> 100 -> 200 用户，每档至少稳定 5 分钟。记录：

| 用户数 | 总 RPS | P95 | P99 | 错误率 | CPU | 结论 |
| ---: | ---: | ---: | ---: | ---: | ---: | --- |
| 20 |  |  |  |  |  | 基线 |
| 50 |  |  |  |  |  | 是否线性增长 |
| 100 |  |  |  |  |  | 是否接近拐点 |
| 200 |  |  |  |  |  | 是否突破 SLO |

容量不是“压到报错时的数字”，而是满足 SLO 的最大稳定点。例如 100 用户时总吞吐 420 RPS、P95 430 ms、错误率 0.3%，200 用户时 P95 1.2 s、错误率 3%，则最大稳定容量应取 100 用户附近，并为发布预留 20%-30% 余量。

### 15:00-16:00：定位项目里的三个可解释瓶颈

1. **搜索慢查询**：请求 `q=slow` 将 Postgres mock 延迟提高到约 130 ms。若 `GET /api/products?q` 的 P95 同时上升，而其他接口稳定，先查搜索 SQL、索引、分页和返回字段。
2. **下单长尾**：支付依赖均值约 120 ms、抖动 45 ms，并有 1.5% 模拟失败。下单 P99 和 502 同时异常时，先看支付调用、超时、重试和幂等，而不是只调应用线程数。
3. **详情缓存旁路**：约 25% 请求会额外访问 Postgres。若详情 P95/P99 比 P50 明显高，检查缓存命中率、热点 Key、旁路查询和数据库连接池。

分析脚本只使用 Locust CSV 计算事实：端点请求数、错误率、平均/P95/P99、峰值/平均 RPS。它生成的“优先排查假设”不是最终结论，必须用 Grafana、日志和 trace 做下一轮单变量实验。

### 16:00-17:00：理解 AI Workflow 的边界

AI Workflow 的正确顺序：

```text
原始 CSV + Prometheus 时间窗
    -> 清洗和聚合（确定性代码）
    -> 证据包（JSON，含单位、时间窗、版本、环境）
    -> LLM 结构化分析
    -> 人工审核事实/假设
    -> 选择复测实验
```

不要把原始海量日志直接丢给模型，也不要让模型替你计算 P99。项目中的 `optional_ai()` 只把分析后的 JSON 发送给模型，并明确要求区分事实和假设、禁止编造监控数据。没有 `OPENAI_API_KEY` 时仍能生成确定性报告。

生产化时还应加入：数据脱敏、成本/超时控制、提示词版本、JSON Schema 校验、失败降级、审计日志和人工审批。

### 17:00-18:00：写出面试可复述项目

用 STAR 结构，控制在 90 秒：

> **背景**：游戏商城大促前，需要验证首页、搜索、详情、购物车、下单关键链路的容量和稳定性。
>
> **任务**：根据线上访问比例建立流量模型，以核心接口 P95 < 500 ms、错误率 < 1% 为 SLO，找出最大稳定容量。
>
> **行动**：我用 Locust 按 35/20/25/12/8 的业务权重生成场景，统一请求命名并校验业务失败；服务侧用 Prometheus 暴露 RED 和 Redis/Postgres/支付依赖指标，Grafana 做同时间窗关联；阶梯增加用户数，CSV 分析脚本输出 P50/P95/P99、RPS、错误率和优先排查假设，再用单变量场景验证慢查询和下游长尾。
>
> **结果**：基线和压力结果可复现，能明确区分应用、数据库、缓存和支付依赖的贡献，并生成复测报告；优化后以相同流量、相同数据集回归，确保 SLO 和错误率没有回退。

简历上的“P95/P99、错误率、资源指标、慢接口/数据库/缓存/下游依赖归因”必须都能在这段话中落到具体查询、文件或实验，否则容易被追问击穿。

## 2. 面试高频问题与回答骨架

### Q1：性能测试和功能测试的区别？

功能测试关注结果是否正确；性能测试关注在给定负载、数据量和环境下，系统是否满足延迟、吞吐、错误率和资源约束。性能脚本仍要有业务断言，否则 200 响应但返回错误数据也会被误判为成功。

### Q2：为什么看 P95/P99，不只看平均响应时间？

平均值会掩盖长尾。1000 个请求中 990 个 50 ms、10 个 3 s，平均值约 79.5 ms，但用户仍会遇到 3 秒请求。P95/P99 更能反映尾部体验和超时风险，关键链路还要看最大值及超时分布。

### Q3：并发数、吞吐量、响应时间有什么关系？

并发是同时在途的请求/用户，吞吐是单位时间完成量，响应时间是单次完成耗时。稳态下可用 Little's Law 近似 `并发 = 吞吐 * 平均响应时间`。Locust 的用户数不是固定 RPS，`wait_time`、服务响应时间和任务比例都会影响实际到达率。

### Q4：如何设计压测场景？

从业务漏斗和线上日志得到请求比例，区分读写、匿名/登录、热 Key/冷 Key、正常/异常和不同数据规模；定义预热、稳态、阶梯、峰值和降载阶段；为每个关键链路设 SLO 和停止条件。脚本中的权重、数据参数和请求命名必须可审计。

### Q5：如何判断瓶颈在应用、数据库还是缓存？

先看应用端点 P95/P99 与依赖 P95 的同时间窗口变化，再结合 CPU、内存、连接池、锁等待、慢查询、缓存命中率和下游错误。单个指标不能证明因果：例如 CPU 低可能是线程在等数据库连接。用拆分 trace 或关闭/替换单个依赖做对照实验。

### Q6：P95 突然升高，你怎么排查？

先确认压测端自身 CPU、网络和连接是否饱和，排除发生器瓶颈；对齐变更、流量、错误率和资源时间线；按端点找最差接口；再拆分应用、数据库、缓存和下游耗时，检查 GC、锁、连接池、队列、重试和慢查询；最后通过降低单一变量或回滚验证假设。

### Q7：为什么要预热？

冷启动包含连接建立、JIT/解释器缓存、代码路径、数据库页缓存和 JVM/Python 内部初始化，不能代表稳态。先用低负载预热，再清楚标记稳态窗口；同时保留冷启动测试，因为弹性扩容和发布后首批请求也有业务影响。

### Q8：如何处理测试数据和幂等？

读请求准备热、温、冷三类数据；写请求使用可回收的租户/用户/订单，避免污染生产；下单需要幂等键，重试不能生成重复订单。压测账号、数据、密钥和个人信息都要脱敏并隔离。

### Q9：Locust 如何避免所有用户打同一个数据？

在 `on_start()` 生成用户身份，在任务里从数据集或随机分布选商品；对热点数据可以有意保留真实热点比例，但要标记这是模型假设。不能无意识地让单一 Key 被所有用户访问，否则缓存和锁结果会失真。

### Q10：如何确定压测机不是瓶颈？

监控压测机 CPU、内存、网络、文件描述符和 Locust worker 数；观察客户端发出请求率是否继续增加、服务端是否还有余量；必要时分布式压测，把控制器和 worker 分开，并校准单 worker 的最大 RPS。压测机饱和时，服务端指标会被低估。

### Q11：错误率怎么定义？4xx 算失败吗？

按业务约定拆分。参数非法等预期 4xx 可以单独统计，鉴权失败、库存不足、支付失败和超时是否算 SLO 失败要提前写进规则。项目里 Locust 显式把非预期状态标记失败，分析脚本再按端点汇总，不把所有 HTTP 200 都当成功。

### Q12：什么时候做稳定性测试？看什么？

在基线和容量测试通过后，用低于容量上限的固定负载运行数小时。看内存/句柄是否持续增长、GC 是否恶化、连接池和队列是否堆积、错误率是否随时间升高、P95/P99 是否漂移。结果要按时间窗口比较，而不是只看最终平均值。

### Q13：性能优化后如何证明有效？

固定代码版本、机器规格、数据集、负载模型、预热和稳态时长，只改变一个优化因素；比较吞吐、P95/P99、错误率、资源和依赖指标的同口径结果。若平均值下降但 P99、错误率或资源成本变差，不能称为成功。

### Q14：AI 在性能测试中具体做什么？

适合做证据摘要、异常聚类、跨指标关联、排查假设和复测建议；不适合替代指标采集、精确分位数计算、生产自动扩容决策。我的实现先用确定性脚本把 CSV 转成带单位和时间窗的 JSON，再让模型解释，并由人工审核和复测闭环。

### Q15：你的项目有什么不足？

诚实说明：教学版本用内存数据和延迟 mock，没有真实数据库、分布式服务、链路追踪和 CI 压测门禁；下一步会接入真实测试环境、OpenTelemetry trace、数据库慢查询采样、分布式 Locust worker，并把 SLO 阈值纳入 GitHub Actions 的质量门禁。能指出边界比声称“完全模拟生产”更可信。

## 3. 你必须能现场解释的命令

```powershell
# 启动服务和监控
docker compose up --build -d

# 小流量基线
docker compose run --rm locust -f /mnt/locust/locustfile.py `
  --host http://service:8000 --headless -u 20 -r 5 -t 2m `
  --csv /mnt/results/baseline

# 分析结果
docker compose run --rm analyzer python /app/scripts/analyze_results.py `
  --stats /results/baseline_stats.csv `
  --history /results/baseline_stats_history.csv `
  --output /results/baseline_report.md

# 清理环境
docker compose down
```

## 4. 面试前最后检查

- 能画出负载层、服务层、依赖层、监控层和分析层。
- 能解释为什么用业务权重，而不是只压单接口。
- 能区分并发、RPS、平均值、P95、P99、错误率和资源饱和。
- 能说出一次“指标 -> 假设 -> 对照实验 -> 结论”的完整排查过程。
- 能说明压测前的授权、数据隔离、限流和停止条件。
- 能承认 mock、单机、无 trace 等项目边界，并给出生产化下一步。
- 简历上的每一个数字都能指出生成它的脚本、CSV 列或监控面板。
